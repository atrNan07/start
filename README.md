# start
following apna-college tutorial
<br><br>
**1. CONFIG** <br>
```python
git config --global user.name "*username*"
git config --global user.email "*email address*"
git config #to check all
```

**2. clone n status**<br>
git clone *git link* : to clone git repository into local (laptop/ pc)<br>
git status : to check status of git tracking<br>
some others.....: <br>
a. cd *file name* : to change directory (file)<br>
b. ls : shows files that can be seen <br>
c. ls -a: shows all files, the hidden files also, here .git means git is tracking that file<br>
